/**
 * Drag and drop configuration constants
 */
export const DRAG_CONFIG = {
  /** Distance in pixels before drag activates (prevents accidental drags) */
  ACTIVATION_DISTANCE: 8,

  /** Delay in milliseconds before drag starts */
  DRAG_DELAY: 0,

  /** Opacity of widget while dragging */
  DRAG_OPACITY: 0.5,
} as const;
